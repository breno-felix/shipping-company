<h3>
  <%= link_to @carrier.brand_name, carrier_path(@carrier) %>
</h3>

<h4>Cadastrar Prazos por:
  <%= link_to 'Km', new_carrier_deadline_path(@carrier) %>
</h4>

<h3>Tabela de prazos por intervalos de Km</h3>

<% if @deadlines.any? %>
  <table>
    <thead>
      <tr>
        <th>Km Inicial</th>
        <th>Km Final</th>
        <th>Valor Mínimo</th>
      </tr>
    </thead>
    <tbody>
      <% @deadlines.each do |p| %>
      <tr>
        <td> <%= p.initial_kilometer %> </td>
        <td> <%= p.final_kilometer %> </td>
        <td> <%= p.deadline %> </td>
      </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p> Não existem prazos por intervalo de Km cadastrados. </p>
<% end %>